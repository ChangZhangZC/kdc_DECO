# Agents Guide

## 项目与关键路径

将 DECO 集成到基于 LeRobot 的 Kuavo 数据转换、训练和部署流程，遵循现有接口与项目结构。

- `DECO/`：原生模型；`kuavo_train/wrapper/policy/deco/`：DECO 策略、配置及预处理适配。
- `kuavo_data/`：数据转换；`kuavo_train/`：训练入口；`kuavo_deploy/`：评估与部署。
- `configs/{data,policy,deploy,accelerate}/`：各阶段配置。
- `lerobot_patches/`：LeRobot 兼容补丁；`third_party/lerobot/`：第三方框架。
- `PLANS.md`：当前有效方案与任务状态；`AI_Logs.md`：变更记录；`Content/`：历史讨论与技术参考。

## 授权与协作

- 用户明确要求的修改视为授权；要求“先给方案”时只分析和提案，等待确认后再修改。
- 在已批准范围内连续完成修改、必要检查及日志更新，不逐文件或逐步骤重复审批；实质扩大范围或改变已确认方案时，再说明差异并确认。
- 复杂改动先简述逻辑、涉及文件及必要的输入输出；仅有实质取舍时说明优缺点。反馈后只说明调整部分，不使用固定确认话术或限定批准词。

## 按需读取上下文

- 接续开发时读取 `PLANS.md` 当前有效且相关的章节及最近相关日志；历史内容先检索再按需展开，避免全量读取或在上下文有效且文件未变时重复读取。
- 纯文档或文本任务只读取相关文本。`Content/` 与旧日志仅作参考，不覆盖用户最新决定或当前有效架构；有影响当前任务的歧义时再核实。
- 仅在策略注册、配置或训练接口集成任务中参考 ACT/DP 对应实现；参考其接口惯例，不将其算法设计强加给 DECO。
- 仅使用与当前任务直接相关、能改善结果的技能。

## 开发与执行边界

- 本机仅允许文本读取、搜索、编辑及 Git 状态和差异检查（如 `git diff --check`）。禁止运行项目代码、临时验证脚本、测试、训练、仿真、ROS 或部署程序，也禁止安装依赖及改变系统环境。
- 禁止使用 `rm` 删除文件，也不得通过其他工具绕过删除限制；确需删除文件时说明路径和原因，由用户手动执行。
- LeRobot 定制修改集中在 `lerobot_patches/`，不直接修改 `third_party/lerobot/`。
- 配置遵循 data、policy、deploy 等现有分层；数据转换保持已有 ROS bag 格式兼容；Python 修改兼容 `requirements_*.txt` 与 Dockerfile 定义的环境。
- 使用中文解释非显然逻辑，重点注明张量维度、单位、坐标系与兼容性约束；普通代码无需逐行复述，注释随逻辑更新。

## 检查与交付

- 静态审查改动及必要依赖，检查接口、逻辑与结构一致性；纯文档改动只检查相关文本，不扩展为全仓库代码审查。明确未运行验证的部分，不把静态检查表述为运行通过。
- 实际修改完成后，在 `AI_Logs.md` 中新增一条中文记录：文件、关键变更、原因、检查结果及未验证项。沿用日期二级标题、任务三级标题和现有日期倒序；不逐条记录工具调用，不改写历史日志。纯分析或咨询不写日志。
- 仅关联计划任务状态发生变化时更新 `PLANS.md` 对应复选框；静态实现完成不代表训练、仿真或实机验收完成。
- 交付时简述改动、用途、检查结果及必要限制；解释深度随任务复杂度和用户教学需求调整。
